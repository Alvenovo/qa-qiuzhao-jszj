# 会话交接

最后更新：2026-08-23

## 这是什么

桌面独立项目：`C:\Users\王文东\Desktop\秋招网站`  
目标 GitHub 仓库：`https://github.com/Alvenovo/qa-qiuzhao-jszj`

2027 届秋招 · 测试开发作战系统。岗位数据只收录江苏/浙江/安徽/上海、软件测试相关岗。

## 当前进度

- 岗位/简历/匹配/ROI/每日任务/模拟面试均保留（`matchModel: v3`）
- **2026-08-23 已补智联 16/16 条原文 JD + 薪资**（智联校园 + 智联社招）
  - 站点 **169 条**（去重 8 条后）
  - **24 条有原文职责，26 条有薪资**
  - 来源：国聘职位详情、智联校园/社招职位详情
- **Boss 直聘未抓到**：网页端风控（API 返回 code 37「环境异常」、详情页被重定向首页、搜索页 75% 空壳），自动化无法绕过；后续在 Boss App 手动看或换时段再试
- 匹配：有原文的岗走职责/技能；没有的仍走方向对照，不把分数做高

## 登录态抓取脚本

`scripts/enrich_login_jobs.py`（本轮新增）：
- 附加调试 Chrome：`chrome --remote-debugging-port=9222 --user-data-dir=<非默认目录>`，然后 `python scripts/enrich_login_jobs.py --port 9222`
- Chrome 136+ 限制：默认 profile 目录不接受 `--remote-debugging-port`，必须用非标准 `--user-data-dir`（复制 profile 后 cookie 加密失效，需在调试窗口重新扫码登录一次）
- 智联校园：`xiaoyuan.zhaopin.com/search` → 卡片 `.position-card` → 详情 `xiaoyuan.zhaopin.com/job/{jdno}`（jdno 在 data-sensors-exposure-option）
- 智联社招：`sou.zhaopin.com` → 卡片 `.joblist-box__item` → 详情 `zhaopin.com/jobdetail/{id}.htm`
- Boss：搜索框输入+回车带重试，卡片 `li.job-card-box`；详情页风控打不开时用搜索页薪资保底
- 去重：同公司+同岗位规范化后只留最详细一条（`--no-dedupe` 关闭）

## 环境状态

- localStorage：`qiuzhao-interview-state`
- 国聘详情缓存：`iguopin_jobs.json`
- 全量：`python scripts/update.py`
- 只补 JD：`python scripts/update.py --enrich-only`
- 登录态补智联/Boss：`python scripts/enrich_login_jobs.py --port 9222`
- 国聘浏览器抓包脚本：`scripts/import_iguopin_dump.py`（依赖当次 CDP dump，非常规入口）

## 本轮代码改动

- `scripts/update.py`：薪资正则支持小数（`1.5-2.5万·14薪`）；`looks_real_jd` 增加「测试出现 ≥2 次」判定
- `scripts/enrich_login_jobs.py`：新增登录态抓取（智联校园/社招/Boss），带去重

## 抓取节奏（按宿舍电脑）

- 工作日公开源交给 GitHub Actions（北京时间 12:00）
- 智联/Boss 登录态详情：回宿舍后说「今晚更新」再抓（Cursor 浏览器里目前只有国聘，Boss/智联未在本轮打开）

## 下一步待办

- Boss 直聘 28 条仍缺 JD/薪资：建议 Boss App 手动查看或换时间段再跑脚本
- 用真实简历走一遍有原文 JD 的岗（如绿盟南京测开、航发测试技术岗）

## 不要做的事

- 不要把招聘网站账号密码写进仓库
- 不要把本项目塞进 `自动化study` 学习仓库
- 不要用整体加分/保底分把匹配度做高
- 不要从招聘首页抽取职责/薪资
- 不要声称某题是该公司真实面试题
